import ModalContent from './Content';
import ModalRoot from './Root';
import ModalTrigger from './Trigger';

const Modal = {
  Root: ModalRoot,
  Content: ModalContent,
  Trigger: ModalTrigger
};

export default Modal;
export { Modal, ModalRoot, ModalContent, ModalTrigger };
