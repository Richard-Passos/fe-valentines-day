import FormControl from './Control';
import FormReset from './Reset';
import FormRoot from './Root';
import FormSubmit from './Submit';

const Form = {
  Root: FormRoot,
  Control: FormControl,
  Reset: FormReset,
  Submit: FormSubmit
};

export default Form;
export { Form, FormRoot, FormControl, FormReset, FormSubmit };
